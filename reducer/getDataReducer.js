import * as types from '../action/types';

const initialState = {
    data: [],
    dataFetched: false,
    isFetching: false,
    error: false
}
  
export  const dataReducer123 = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state
    }
}